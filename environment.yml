# This file may be used to create an environment using:
# $ conda env create --name <env> --file environment.yml
# platform: linux-64
channels:
  - conda-forge
  - bioconda
dependencies:
  - python=3.9
  - numpy
  - scipy
  - seaborn
  - pybedtools=0.9.0
  - pysam=0.20.0
  - pandas=2.0.0
  - matplotlib=3.7.1
